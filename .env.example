# Backend API base URL for the admin panel.
# In production on the VPS, nginx proxies /api → FastAPI, so just use the same origin:
# VITE_API_BASE=/api
#
# For local dev pointing to the live backend:
VITE_API_BASE=https://tg.nexus-x.site/api
